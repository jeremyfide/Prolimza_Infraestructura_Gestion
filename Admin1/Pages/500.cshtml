@page
@model Page500Model
@{
    ViewData["Title"] = SharedLocalizer.GetLocalizedHtmlString("Tables");
Layout = "_LayoutError";
}

<main>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="text-center mt-4">
                    <h1 class="display-1">500</h1>
                    <p class="lead">@SharedLocalizer.GetLocalizedHtmlString("Error interno del servidor")</p>
                    <a asp-page="Index"><i class="fas fa-arrow-left mr-1"></i>@SharedLocalizer.GetLocalizedHtmlString("Volver al inicio")</a>
                </div>
            </div>
        </div>
    </div>
</main>
